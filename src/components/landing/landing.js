import React from 'react';
import Hero from './hero';

export default class Landing extends React.Component {
    render() {
        return (
            <div id="landing">
                <Hero />
            </div>
        );
    }
}