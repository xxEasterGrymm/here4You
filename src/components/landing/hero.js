import React, { PureComponent } from 'react'

export default class Hero extends PureComponent {
    render() {
        return (
            <div>
                hero
            </div>
        )
    }
}
